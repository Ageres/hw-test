apiVersion: v1
kind: Service
metadata:
  name: {{ include "scheduler.fullname" . }}
spec:
  type: {{ .Values.scheduler.service.type }}
  selector:
    app: {{ include "scheduler.fullname" . }}
  ports:
    - port: 8080  
